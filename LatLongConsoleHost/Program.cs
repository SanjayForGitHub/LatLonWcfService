using LatLonServiceContract;
using LatLonWCFService;
using System;
using System.Collections.Generic;
using System.Linq;
using System.ServiceModel;
using System.Text;
using System.Threading.Tasks;

namespace LatLongConsoleHost
{
    class Program
    {
        static void Main(string[] args)
        {
            //StudentRegistrationService StudentRegistrationService = new StudentRegistrationService();
            //var fullName = StudentRegistrationService.Register("Sanjay", "Soni");
            //Console.WriteLine(fullName.Result);
            //Console.ReadLine();
            //using (ServiceHost host = new ServiceHost(typeof(StudentRegistrationService))) {
            //    host.Open();
            //    Console.WriteLine("Host Started: " + host.BaseAddresses.FirstOrDefault().AbsoluteUri.ToString());
            //    Console.ReadLine();
            //    host.Close();
            //}
        }
    }
}
